.build/
.swiftpm/
Package.resolved
*.xcodeproj
DerivedData/
.DS_Store
*.swiftmodule
.netrc

# Conformance harness (dev-only Node tooling). The lockfile IS committed so CI
# uses `npm ci` for reproducible, pinned installs; only the install tree is ignored.
Scripts/conformance/node_modules/
